# Plans index — overnight tanren run, May 2026

Plans written while the long-haul daemon does its thing. Each is sized to be a single
PR's worth of work; sequencing notes call out dependencies. Order = priority.

## Sweep complete — plans 35 + 38 (status as of HEAD `c6d2670`, 2026-05-08 PM)

The coordinated sweep landed the JsonAgent layer (plan 38) and the standards / architecture
dual-bucket (plan 35). Daemon is STOPPED; workspace WIPED; all 12 sweep PRs shipped.
See `orientation.md` §9.3 for the deploy sequence (validate → reinstall → proxy health → re-seed → restart).

| Plan | Sub-PR | Commit | Status |
|---|---|---|---|
| 35 | PR-A — loaders, dual-bucket schema, validators, planner prompt, seed profiles, `qk standards seed` | `4c92f83` | ✅ shipped |
| 35 | PR-B — architecture-alignment auditor (5th gauntlet stage); `pre-pr-architecture.md`; `unreferenced-applicable-*` detectors | `c6d2670` | ✅ shipped |
| 36 | SELF_AUDIT short-circuit aligned-row carve-out | `72e5d39` | ✅ shipped (DEAD CODE — `self_audit.py` deleted by 38 PR-B.5) |
| 37 | `qk monitor` built-in CLI subcommand | `630fa10` | ✅ shipped |
| 38 | PR-A — JsonAgent layer + role-MODEL binding + `cfg.<role>_model` knobs + `qk retry --all-non-merged` + docker `--add-host` | `e896511` | ✅ shipped |
| 38 | PR-B.1 — `ArchitectureRefSchema` on `SubtaskSpec` | `3e78050` | ✅ shipped |
| 38 | PR-B.2 — `progress.py` on JsonAgent layer | `5e92240` | ✅ shipped |
| 38 | PR-B.3 — `pre_pr_audit.py` three roles on JsonAgent layer | `ab86614` | ✅ shipped |
| 38 | PR-B.4 — planner / fixup / merge planner on JsonAgent layer; `extract_json` deleted | `5e54004` | ✅ shipped |
| 38 | PR-B.5 — SELF_AUDIT retired; subtask_execution rewritten; prompts rewritten | `683e641` | ✅ shipped |
| 38 | PR-B.6 — `quikode/json_extract.py` deleted | `0fb20ca` | ✅ shipped |
| 38 | PR-B.7 — three remaining `build_agent` call sites migrated; legacy CLI shims deleted (opencode/codex/claude/parse_tokens/AgentResult/AgentRole/build_agent) | `278e7d6` | ✅ shipped |
| 38 | PR-C — TUI live-state correctness + config audit log + template-vs-default invariant + `record_agent_call_started/finished` pair | `794ac56` | ✅ shipped |

| # | Title | Why it matters | Depends on |
|---|---|---|---|
| 01 | retry-landscape | Landscape map. Read first. | — |
| 02 | network-rate-limit-backoff | gh/git polls have no backoff; one 429 → infinite retries. **Shipped** as `quikode/net_retry.py`; wired into github.py + github_graphql.py. | — |
| 03 | conflict-cap-and-force-push | Hard-coded 6 ignores config; force-push has no retry. | 02 |
| 04 | supervision-stall-event | Stall reset uses wrong FSM event, loses evidence. | — |
| 05 | poisoned-worktree-wipe | "Wipe rather than carry forward" — the user's explicit ask. | 06 |
| 06 | progress-check-signal-quality | Deterministic locality fingerprint, not just agent verdict. | — |
| 07 | observability-overnight | Heartbeat-watch + cluster detection in briefing. | — |
| 08 | budget-tightening | Cut hard_max from 50 to 20 once 05+06 are in place. | 05, 06 |
| 09 | prompt-level-leverage | Doer/triage/planner edits to reduce retry rate. | — |
| 10 | resume-correctness | Orphan recovery edge cases: worktree, planner staleness. | — |
| 11 | stream-agent-output-to-log | `qk tail` is currently silent for 10+ min until the agent exits. Tee `Popen` to log. | — |
| 12 | no-ci-leak-invariant | Prompt-level fix for R-0005 BLOCKED. Bakes "branch owns every commit" / "no pre-existing failure" into doer/triage/checker/planner/progress. Shipped. | — |
| 13 | scope-review-gate-fix-rule | Follow-up to 12. Scope reviewer was nuking the cross-file fixes the new doer/triage prompts authorize, leaving the system in a churn loop. Added "gate-keeping cross-file fixes are ALWAYS legitimate" to `scope-review.md`. Shipped. | 12 |
| 14 | checker-must-not-fabricate | Walked back plan 12's "synthetic FAIL bullet" guidance for the subtask checker. It was inventing criteria the doer couldn't satisfy (e.g. R-0021/S-08 spent 11 attempts because the checker fabricated a "surface startup" criterion the BDD scenario already mocked via in-process harnesses by tanren design). New rule: fail on observed gate failures only; never fabricate. Shipped. | 12 |
| 15 | qk-show-markup-safety | `qk show` crashed with `MarkupError` whenever an artifact contained bracketed paths like `[/workspace/...]` (rustc, BDD, checker output). Operator/agent couldn't read triage at all. Fix: print artifact bodies with `markup=False, highlight=False`. Shipped. | — |
| 16 | dev-container-readiness-timeout | 16-way parallel boot of dev containers exceeded the 60s `wait_dev_ready` ceiling under I/O contention; tasks marked FAILED while their containers actually finished entrypoint a moment later, then held the budget as zombies. Fix: bump call-site to 240s and default to 120s. Shipped. | — |
| 17 | clean-loop-prompt-architecture | R-0005/S-07 spent 18 attempts in a triage↔scope oscillation. Triage was prescribing "remove file X" (overstepping into scope + doer judgment); scope reviewer read prior-attempt triage notes (wrong input) instead of the current doer's summary; checker re-ran the gate the objective check already covered. Rewrote do/scope/check/triage prompts from scratch with single responsibilities; scope reviewer now reads `doer_summary` not `triage_notes`. Shipped. | — |
| 18 | doer-must-inspect-actual-diff | Plan 17's design surfaced a deeper bug on the first live test: a doer wrote "no out-of-lane edits" while the worktree carried a persistent migration fix from prior attempts (scope rejection unstages but doesn't revert files — by design, to preserve work). Doer prompt now requires running `git status -uall` + `git diff HEAD --stat` before writing the summary and reconciling every file in the diff (keep / keep+justify / fix-in-place). No worktree-revert in the orchestrator: per the user's "fix in place, don't discard work" standing preference. Shipped. | 17 |
| 19  | quota-cascade-prevention (overall) | **Partially shipped.** Hard constraint: NO cost-to-subscription approximation (tokens-per-bucket varies 5× peak/off-peak, 2× weekend/weekday, plus provider promotions/load — local accounting is wrong by an unbounded factor). Real-signal monitoring only, à la codexbar. 19A shipped (below); 19B (`qk usage` + briefing line) and 19C (model rotation chain) remain queued. | — |
| 19A | quota-exhausted-internal-retry | The minimum cascade-prevention fix: `_exec` in agents/base.py now detects subscription-quota signals (Claude session/weekly/Opus, Codex `rate_limit_exceeded`, generic 429, "rate-limit/quota/usage-limit … exceeded/reached/exhausted/hit") on rc != 0, sleeps with exponential backoff (5min → 30min cap, 8h cumulative), and retries inside the agent layer. The FSM never sees the quota result, so the doer→checker→triage cascade never starts. Per-call `timeout` watchdog stays orthogonal — hung agents still die via `TimeoutExpired` exactly as before. | 19 |
| 20  | container-vanished-recovery | 2026-05-07 incident: 12 tasks BLOCKED at 50/50 retries + 4 FAILED with `InvalidTransition` after a host-wide docker incident + an orchestrator race. Four patches: (1A) `subtask_execution._run_subtask_check_command` classifies objective-check stderr against `_TRANSIENT_STDERR_MARKERS` so vanished-container gate failures are free retries; (1B) new `docker_env.ensure_dev_container_running` called per-attempt re-provisions dead containers; (1C) `task_worker._safe_crash_current` skips `crash_current` when row is already in TERMINAL_STATES, breaking the FSM cascade; (1D) `cli_core` acquires an exclusive `fcntl.flock` on `<state_dir>/orchestrator.lock` before any destructive workspace prep so two `qk run` invocations can't race. Plus new `qk reset-retries <task> [<subtask>]` CLI for surgical post-incident recovery. | — |
| 21  | scope-review-observability | Scope review was the only agent role that didn't record to `agent_calls` or persist an artifact — operator and triage agents could only infer rejections from prose in `commit_subtask`'s output. Surfaced when R-0020/F-1-7's triage named "scope reviewer" as the failure layer and `qk show` had no scope_review history at all. `ScopeReviewResult` now carries the captured `AgentResult` + role; `_lane_review` records both `agent_calls(phase="subtask_scope_review")` and a `subtask_scope_review:<id>` artifact whenever the agent ran. Pure additive. Shipped. | — |
| 22  | doer-prior-output-carry-forward | R-0005/S-10 hit 47 attempts on a multi-file BDD harness debug task because each 22-min doer turn restarted investigation from scratch. The agent infra already preserves partial stdout on timeout (`agents/base.py:214-237`) and persists it as the `subtask_doer:<id>` artifact, but the next attempt's prompt didn't see it. Now `_do_subtask` fetches the latest prior artifact (trailing 6000 chars) and renders it under "Your prior attempt's output — continue from where you left off". Worktree state was already preserved across attempts (`git reset HEAD -- .` only unstages, never reverts files); this closes the *prompt-context* gap. Doer timeout intentionally NOT bumped per user direction "we'd rather carry forward progress." Shipped. | — |
| 23  | same-signature-stop-loss | R-0005/S-10's 47 retries dodged the flatline detector because each attempt's output was different-but-equally-invalid; the progress-check agent kept rating it "progressing". But every retry_reasons row landed with identical `(category, signature) = ("doer_output_invalid", "rc=0")`. New deterministic block path: `subtask_same_signature_block_count` (default 5) — when the last N non-transient retry_reasons share a tuple, BLOCK regardless of progress-check verdict. Independent of and complementary to the existing flatline detector; transient retries excluded so infra noise doesn't trip it. Shipped. | — |
| 24  | final-stabilization-subtask | Cycle 1 of every pre-PR audit gauntlet was nearly always failing local-CI because per-subtask checks run only the fast gate (`just check`), and cross-subtask integration breakage isn't visible until everything lands. Each cycle-1 failure burned a fixup-planner round on issues the last spec-subtask's doer was best positioned to fix. New deterministic Z-99 stabilization subtask appended programmatically (not via planner prompt) at parse time; depends on every prior subtask, files_to_touch=[] (every edit is gate-fix → scope review handles), acceptance is "spec gate passes rc=0". Companion doer-prompt rule: test-authors must run their tests before stopping. Shipped. | 13, 21 |
| 25  | pre-pr-cycle-resume-resilience | Queued. R-0020 / R-0041 regression: when the daemon SIGTERMs mid-pre-pr-cycle (during/just-before the behavior audit, or while the worker is in `local_ci_checking`), orphan recovery resets the task to PENDING but `pre_pr_audit_summary` retains the partial cycle's results AND any fixup subtasks the planner may have already emitted. On resume, the worker fast-forwards through completed subtasks → enters `local_ci_checking` → calls `_run_pre_pr_pipeline` with `begin_pre_pr_audit_cycle(1)` which **wipes** the partial summary, OR (worse) the worker finds non-DONE F-* subtasks in the DB and re-enters the subtask loop, completely bypassing the missing audit stages. Fix: detect partial-cycle on resume (any stage with `passed=null` after a recovery) and either re-run only the queued stages, or wrap each cycle in atomicity (try/finally that records "cycle aborted" so resume knows to redo). Queued — needs design between options. | — |
| 26  | tui-gauntlet-staleness + z99-resume-guard + scroll | Three R-0021 forensics fixes: (1) TUI rendered the pre-PR gauntlet panel even when current FSM state was `doing_subtask` running Z-99-stabilize-spec-gate, showing a 25-hour-old cycle 1 summary as if current — `_gauntlet_block` now also requires state ∈ pipeline-relevant set; (2) on resume, plan 24's Z-99 was being re-injected even when the task was already past spec phase with in-flight `F-1-*` fixups, scheduling Z-99 attempt 2 alongside `F-1-11` and burning retries on a gate that couldn't pass — the resume parse now skips Z-99 injection when any `kind="fixup-…"` subtask exists for the task; (3) `DetailPanel` `#detail-phase` Static wrapped in `VerticalScroll` so long phase content (multi-line gauntlet + state desc + container stats) is scrollable instead of clipped at panel bottom. Shipped. | 24 |
| 27  | targeted-rewind-recovery | New `qk rewind <task> <subtask>` recovery primitive. Pre-plan-27 toolbox forced a binary choice on toxic-loop subtasks: `qk retry` (wipe everything, lose all prior commits) or `qk resume` / `qk reset-retries` (preserve everything, including the toxic uncommitted state). Rewind is the surgical middle ground: it `git reset --hard`s the worktree to the predecessor subtask's commit (force-pushes the branch by default, `--keep-remote` to skip), resets target + every topo-after subtask to PENDING with cleared retries/triage/commit_sha/retry_reasons, clears `pre_pr_audit_summary`, transitions task to PENDING with the resume marker. Idempotent: doer prior-output artifacts survive so plan 22's carry-forward still feeds the next attempt the prior thinking. Comes with `--dry-run`. Replaces the orientation's "wipe and start over" guidance with a rewind-first recovery flow. Shipped. | 22, 26 |
| 28  | streamlined-post-pr-fsm | Cut the post-PR state machine from six states to three: `MERGE_READY` and `TRIAGING_FEEDBACK` retire; settle window retires; per-thread classifier deleted. Only formal GitHub Reviews (`APPROVED` / `CHANGES_REQUESTED` / `COMMENTED`) trigger transitions — bot/AI-reviewer line comments become bundled CONTEXT, not polling triggers. Two timers: CI-only in PENDING_CI; CI + Reviews in AWAITING_REVIEW. CI failure path bundles failed-check excerpt; CHANGES_REQUESTED bundles all unresolved threads + PR comments + review bodies (resolved threads excluded — human-dismissed = ignore). Auto-merge fires on `APPROVED` when `auto_merge_when_clean=True`. Plan-12 driveby: doer prompt explicitly forbids "pre-existing" / "out-of-scope" disclaimers for failing gates (R-0010 S-07 incident). ~250 LoC removed, ~200 LoC added. Single PR. Shipped. | 12, 13, 17, 23 |
| 30  | review-ready-signal | Reintroduce ntfy.sh delivery via a unified "review-ready-settled" signal: a task is settled when it has been in AWAITING_REVIEW for ≥ `cfg.review_ready_settle_s` (default 15min). The threshold serves two purposes: (1) ntfy notification fires (title + PR url + brief summary; click → PR); (2) stacked-diff dependents become eligible to start. Same threshold so dependents always start from a CI-green parent the operator could have reviewed. Scheduler bumps to **primary-first tier**: stacked children only get scheduled when no primary candidate is pickable (was: stacked got +50 boost). Removes plan-28's intentional drop of the notification surface; resurrects it cleanly without the settle-window FSM hop. Tanren ships set to `stacking_strategy="aggressive"` + `stacking_readiness="settled"` for full cross-milestone chaining. Shipped. | 28 |
| 31  | stacked-diff-rebase-model | Rebuild L1 + L2 rebase semantics around children always staying stacked on parent's evolving tip (PR base = parent.branch), never un-stacking onto main on parent push. Removes the un-stack-onto-main code path entirely (no flag — today's behavior is treated as a wrong implementation that gets replaced). Splits worker entry into `run_rebase_to_parent_tip` (parent advanced; stay stacked) vs `run_rebase_to_main` (parent merged; reattach). Adds cascade-walk-level coalesce. Fixes hardcoded resolver-iteration cap (now `cfg.conflict_resolver_max_iterations`). Adds outer rebase-retry budget `cfg.rebase_max_attempts`. Conflict-resolver prompt extended with `rebase_target_kind` / `parent_diff_excerpt` so the framing matches the actual rebase target. Single PR. **Shipped.** | 30 |
| 32  | merge-node-first-class | Rewrite multi-parent (L3) handling around a synthetic merge-node task per unique parent set: lazily materialized, reused across downstream children, owns its plan→implement→check→audit cycle, no PR. Children with multi-parent deps resolve to a single effective parent (the merge-node's branch) at provisioning time; multi-parent rebase code at the child level is deleted. Cascade: parent advance → merge-node re-runs → children rebase via existing single-parent cascade. Recursive composition (D depends on [A+B, C] → M(M(A,B), C)) — arbitrary DAG depth. No "this conflict is unsolvable, BLOCK" — the merge-node's subloop handles semantic integration via doer/checker. Sequential merge order = downstream child's `Node.depends_on` order. Tanren has 64 multi-parent nodes (27% of DAG); paper-feature today, real after this. Two PRs (storage/FSM/worker, then planner-prompt + integration tests). **Shipped** (PR-A `ebee454` + `e524272` + `a6f3d53`; PR-B `cf9988d`). | 31 |
| 34  | daemon-stop-reliability | 2026-05-08 incident: `qk daemon stop` SIGTERM'd only the supervisor; the inner `quikode.cli run` child survived as orphan and ticked for 12+ min, recreating containers, advancing state, and writing fresh heartbeats while `qk daemon status` reported "not running". `qk reset` saw only a partial container set and nearly went destructive against a live workspace. Three fixes: (1) `daemon stop` walks the supervisor's full child tree via `/proc` ppid scans, SIGTERMs each, waits up to 30s, SIGKILLs survivors (supervisor → children → docker-exec last), unconditionally removes pid+heartbeat files; (2) `daemon status` detects orphan `quikode.cli run` children when supervisor is dead and exits nonzero with a loud WARNING; (3) `qk reset` refuses to run with a live supervisor or orphan, `--force` escape hatch. New `quikode/process_tree.py` + `quikode/daemon_shutdown.py` modules (stdlib-only, no psutil dep). 15 new lifecycle tests. Shipped. | — |
| 33  | rubric-first-information-architecture | Diagnosis: zero PRs in a week despite continuous daemon activity. Every upstream agent (planner, doer, checker, triage, fixup-planner) is graded against a four-stage post-implementation gauntlet (`local_ci`, `rubric`, `standards`, `behavior`) they cannot see — degraded shadow of the real bar. Fix: single shared `EvaluationContract` built at PROVISIONING + persisted per-task + loaded by every prompt-render entry. Schema gains `rubric_targets`, `standards_referenced`, `behavior_evidence_advanced` per subtask; `gauntlet_strategy` per plan; `addresses_findings` retired (folded into stage-typed fields); `files_to_touch` demoted to advisory (no enforcement, no multiplier cap). Three planner validators (rubric coverage, evidence partition, standards-paths). Per-subtask loop becomes: doer codes → mandatory `SELF_AUDIT` block → deterministic short-circuit if `predicted_score<min` or RISK/STUB → otherwise LLM checker (adversarial different model) verifies SELF_AUDIT against diff + runs scoped behavior witnesses → on FAIL, LLM triage (senior-engineer-tutoring-junior, ~25 lines, file:line teaching). Scope review retired entirely (module + prompt + tests + plumbing deleted; `_classify_empty_staging` survives for Z-99 gate-only success). Hard cutover: validator rejects pre-plan-33 plans by construction; mass `qk retry` at deploy including `kind="merge"` rows. Two PRs: PR-A schema + contract + planner/merge-planner + scope-review deletion (~700 LoC); PR-B doer/checker/triage/fixup-planner + SELF_AUDIT parser + witness runner (~600 LoC). Queued. | 12, 14, 17, 18, 22, 23, 24, 26, 28 |
| 38  | json-schema-agent-layer | **Master architectural reset.** Replaces plan 33's SELF_AUDIT contract with a schema-enforced JSON agent layer. Verified all three CLIs (claude / codex / opencode) support JSON output mode at the command line; claude has `--json-schema`, codex has `--output-schema`, opencode emits JSON events with client-side pydantic validation. New `quikode/agent_schemas.py` (one BaseModel per role); new `quikode/agents/json_protocol.py` with a unified `JsonAgent` ABC + three concrete CLI shims; new `quikode/agent_registry.py` decoupling role from CLI (any role × any CLI works — `cfg.planner_cli="opencode"` Just Works). SELF_AUDIT entirely retired: `quikode/self_audit.py` deleted, plan 36's carve-out deleted with it, doer prompt rewritten to emit a lightweight `DoerEnvelope` JSON (bookkeeping only — diff is the evidence). LLM checker rewritten to grade the diff directly against rubric/standards/behavior contract; triage rewritten without `self_audit_mismatch`. Every prose-parsing site retired: `subtask_schema.extract_json`, the three near-duplicate `pre_pr_audit.py` JSON extracts, `progress.py:154` heuristic, `_CODEX_TOKENS_RE` / `_GENERIC_TOKENS_RE` / `parse_tokens` — all deleted, callers wired to `JsonAgent`. TUI overhauled to display agent_call live-state independently of FSM state (no more "running per-subtask doer" when the call has actually returned). Stale-config audit log added at daemon-start; `config_template.py` seeds bumped to match Field defaults; regression test locks template-vs-default invariant. Three PRs (~1500 + ~1500 + ~600 LoC). Mass `qk retry` at deploy — bundles with plan 35 (standards profiles) for one restart. Queued. | 33, 35, 36 |
| 37  | qk-monitor-builtin | Promote the operator's `/tmp/qk-monitor.py` state-log poller into a first-class `qk monitor` subcommand. Replaces the flaky `tail -F daemon.log \| grep` pattern with a SQLite-direct poll of `<state_dir>/quikode.db`'s `state_log` table; emits one line per interesting transition (default filter: pr_opening / pending_ci / awaiting_review / merge_ready / merged / blocked / failed / triaging_feedback / addressing_feedback / rebasing_to_main + note keywords + `attempt ≥ 6` soft-cap regex). New `quikode/cli_monitor.py` module wired into the Typer dispatch alongside other read-only commands; uses direct `sqlite3` read-only `mode=ro&uri=true` connection (not `Store`, which opens RW and runs migrations). Flags: `--since <duration>`, `--task <id>`, `--all`, `--keywords <list>`, `--interval`, `--format {line,json}`, `--once`. Cursor persisted to `<state_dir>/qk-monitor.lastts` (per-workspace, not `/tmp`). Default tails forever, line-flushed, SIGINT-clean — matches Claude Code Monitor-tool semantics. Works whether or not the daemon is running. orientation.md §6 rewritten to point at `qk monitor`; `qk briefing` Hints footer gains a line. ~200 LoC + ~80 LoC tests, single PR, stdlib-only, no schema/FSM/prompt change, no `qk retry`. Queued. | — |
| 36  | self-audit-denial-false-positive | R-0023 / S-03-cli-install-entrypoint hit retries=6 because the doer's `gate_standards` row read `aligned (... no unwrap, expect, panic, todo, unimplemented)` — denying the deny-listed tokens — and plan 33's deterministic risk-token scan (`\b(RISK\|STUB\|TODO\|FIXME\|XXX)\b`) caught the literal `todo` and FAIL_FAST'd with `failure_layer="self_audit_mismatch"`. Structural false-positive: any subtask whose standards profile names banned tokens (e.g. `error-handling.md`) recurs the same way; the doer cannot avoid mentioning deny-listed tokens when citing the standard. Fix: `_scan_risk_tokens` skips `gate_standards` rows where `aligned=True` (the row is by construction a denial of drift; LLM checker still verifies aligned claims against the diff). Drifted standards rows still scanned (drift admissions are real signal); rubric and behavior rows unchanged. ~10 LoC + 2 regression tests. No schema/prompt/migration change; no `qk retry` needed at deploy — daemon restart picks up the fixed parser. Single PR. Queued. | 33 |
| 35  | standards-profile + architecture-alignment linking | **Queued — DO NOT SHIP IN ISOLATION.** Forces a mass `qk retry` per plan-33 D11; bundle with other resets. Ship only when (a) human review is rejecting tasks for bucket-confusion reasons, or (b) another plan also needs a reset and 35 can ride along, or (c) current run reaches human-review-ready and surviving issues localize to planner-prompt quality. R-0002 produced `standards_referenced` entries pointing at tanren *system-architecture* docs (`docs/architecture/subsystems/identity-policy.md§Permissions`) instead of *standards profiles* (`rust-cargo`, `react-ts` — how Rust/Cargo and React/TS code should be written). Architecture-alignment is real and load-bearing — likely *more* important than standards alignment — but it was misclassified into the standards slot because `cfg.pre_pr_standards_profile_globs` defaults to `docs/architecture/**/*.md`, the contract loads those docs as standards, the planner sees that blob, the validator only checks file existence, and the standards audit grades the diff against architecture prose. Tanren has real standards profiles at `tanren/profiles/{rust-cargo,react-ts-pnpm,default}/{global,rust\|react\|typescript,architecture,testing}/*.md` with YAML frontmatter that quikode never loads. Fix is additive, not replacement: keep both bars, give each its own slot. New `Subtask.architecture_referenced` field parallels `standards_referenced`. Pre-PR gauntlet grows from `local_ci/rubric/standards/behavior` to `local_ci/rubric/standards/architecture/behavior`. New `quikode/standards_profiles.py` loader (frontmatter-aware) and `quikode/architecture_docs.py` loader (free-form). New configurable knobs: `standards_profiles_dir`, `standards_profiles`, `architecture_docs_dir`, `architecture_doc_globs`, `architecture_path_map`; `pre_pr_standards_profile_globs` retired. `EvaluationContract` gains `architecture` slot alongside standards (each 30k cap); `ec_targeted` inlines cited section bodies for both. Two new validators: `validate_standards_refs` (rejects non-profile paths with a bucket-correction message), `validate_architecture_refs` (rejects paths outside `architecture_docs_dir`). New `prompts/pre-pr-architecture.md` audit prompt parallel to `pre-pr-standards.md`; both audits emit `unreferenced-applicable-*` findings when the diff touches a path matching a profile's `applies_to` (or a subsystem doc per `architecture_path_map`) but no subtask cited it. Ships starter `rust-cargo` + `react-ts` seed profiles inside quikode (no architecture seed — that's project-specific). New `qk standards seed` CLI to copy them out. Hard cutover per plan 33 D11: legacy config key raises, bucket-routing errors re-prompt then BLOCK, mass `qk retry` at deploy. Two PRs (~700 LoC schema/contract/validators/planner; ~500 LoC audit-side + new prompt). Queued. | 33 |
| 39  | fixup-output-repair-budget | 2026-05-09 BLOCK cascade: fixup planner outputs were useful but rejected after one validator re-prompt because `validate_finding_coverage` treated extra rubric/behavior stage claims as fatal and fixup citation validation required exact section headings such as `§Rules`. Fix: expected findings remain mandatory and duplicate ownership still fails, but extra stage claims no longer block. Fixup standards/architecture refs are doc-bucket strict and section-alias lenient; spec planner refs stay section-strict. Added `fixup_planner_output_retries` default 5 so schema/validator output violations retry in-place before BLOCKED is used as the escape hatch. Shipped. | 33, 35, 38 |
| 40  | rubric-fail-forward-all-gaps | When the rubric audit fails overall, include concrete `gaps_to_reach_ten` from above-threshold categories as `rubric_reach_ten_gap` findings. The fail decision still depends only on categories below `pre_pr_rubric_min_score`; fully passing rubric audits do not emit fixup findings. Shipped. | 33 |
| 41  | quality-gate-budgets-and-release-valve | Standards and architecture audits now gate on configurable severity budgets (default: one medium allowed, zero high/critical), matching rubric's tunability. Added a pre-PR release valve: after configured cycles, if local CI and behavior pass and only deferable quality stages remain failing, open the PR and include deferred findings in the PR body. Config/bootstrap/infra/transport/parse failures, local-CI failures, behavior failures, and critical findings remain blocking by default. Shipped. | 33, 35, 40 |
| 42  | pre-pr-audit-output-retries | Rubric/standards/architecture/behavior audit schema failures are now treated as retryable output violations before synthetic `parse_failure` is emitted. Added `pre_pr_audit_output_retries` default 5, seeded in new workspace configs, loaded from TOML, and covered by tests. This prevents malformed audit JSON from becoming a fixup/block cascade while preserving fail-closed behavior after the retry budget is exhausted. Shipped. | 38, 39, 41 |
| 43  | structural-audit-failures-block | Pre-PR audit structural failures (`parse_failure`, transport/infra/config/render/bootstrap) now BLOCK after retries instead of entering the fixup planner, because target-repo subtasks cannot fix quikode/runtime output failures. Shutdown now sets a shared stop flag; audit results returned after shutdown is requested are discarded so SIGTERM-interrupted agents cannot poison the next cycle with fake parse-failure findings. Shipped. | 42 |
| 44  | codex-auth-refresh-race-retry | Codex-direct OAuth refresh races (`token_revoked`, `refresh_token_reused`, invalidated OAuth token text) are now classified as transient agent-auth failures and retried inside the JSON transport with short backoff. Exhaustion surfaces as `rc=124 transient=True`; initial planner calls get `planner_retries_on_transient`; triage rc failures are rendered as transport failures instead of parse failures. Shipped. | 19A, 38 |
| 45  | codex-native-empty-output-reprompt | Codex-direct native schema calls that exit `rc=0` with an empty `--output-last-message` now get one immediate schema repair prompt inside the JSON protocol layer instead of burning a real subtask retry and cascading into same-signature stop-loss. Shipped. | 38, 44 |
| 46  | glm-zai-wafer-fallback | `GLM-5.1-zai` now declares `GLM-5.1-wafer` as a quota fallback. Fallback-capable LiteLLM primaries surface 429/quota immediately, `QuotaFallbackJsonAgent` invokes the next provider for the same role call, and the live Tanren doer config is switched back to `GLM-5.1-zai`. Shipped. | 19A, 38 |
| 47  | retire-doer-envelope | Retire the `DoerEnvelope` JSON contract: doer transports run plain apply-patch (no `--output-schema`/`--json-schema`), the diff is the sole evidence, and Plan 22 carry-forward flows through `triage_notes` only. Removes `DoerEnvelope` schema/registry/prompt/test surface; `WritesFilesAgent` accepts `envelope_schema=None` and dispatches to a new `transport.invoke_raw`; `ConflictResolverEnvelope` stays. Queued. | 38, 46 |
| 48  | stop-loss-signature-granularity | Plan 47's diff-only contract collapsed every checker FAIL to an identical `(doer_output_invalid, rc=0)` retry signature, tripping plan-23's same-signature stop-loss after 5 attempts regardless of true failure layer. `retry_classify.classify_retry` now consumes the structured `Verdict` and `SubtaskTriageOutput.failure_layer` directly — `_CHECKER_VERDICT_RE` rendered-text scraping is removed — so signatures become `verdict=FAIL,layer=local_ci\|rubric\|standards\|architecture\|behavior`. `_triage_subtask` returns `(text, failure_layer)`, `_record_subtask_triage` plumbs both through `_append_retry_reason`. `qk resume` additionally clears `retry_reasons`/`retries`/`transient_retries`/`flatline_count`/`progress_check_count` on subtasks whose original state was `blocked`, so a manual resume gives a fresh stop-loss budget. No backwards-compat shims. Shipped. | 23, 38, 47 |
| 50  | config-loader-orphan-fields | `Config` declared 5 fields (`subtask_same_signature_block_count`, `subtask_witness_timeout_seconds`, `fixup_planner_timeout_s`, `fixup_planner_retries_on_transient`, `pre_pr_architecture_model`) that `load_config` never read; TOML overrides silently fell back to Field defaults while `_log_int_overrides` cheerfully audited the override as if it had taken effect. Discovered 2026-05-10 trying to bump `subtask_same_signature_block_count` 5→10 to soften a stop-loss cascade — daemon log said 10, cfg held 5, stop-loss kept tripping at 5. The 15s witness cap (workspace toml set 180s) is the most operationally significant orphan. Fix: add the 5 missing reads to `load_config` matching surrounding style; new post-construction audit `_warn_orphan_overrides` walks `Config.model_fields` for any top-level toml key whose final cfg value still equals the default and fires `log.warning` so future drift can't hide. Tests cover each orphan's override taking effect plus the audit's positive/negative paths. Source-repo only — workspace toml already has the bumps queued; manager handles deploy. Shipped. | — |
| 49  | review-watcher-state-guard | 2026-05-10 daemon crash loop on R-0040: `_handle_post_pr_ci_failure` and `_handle_changes_requested` unconditionally fired `fsm_runtime.enter_addressing_feedback`, but the FSM correctly rejected `blocked → addressing_feedback` (operator must unblock first). The orchestrator child crashed rc=1 and the supervisor restarted into the same crash on every poll. Both call sites now check the task's current state before firing the FSM event; if `BLOCKED`/`FAILED`, log at INFO and skip — for the CHANGES_REQUESTED path, the review cursor is also NOT advanced so the next poll picks the review back up once the operator unblocks. CI-failure path returns `False` so the main loop continues to invoke later candidates' handlers. CHANGES_REQUESTED path re-reads task state via `Store.get` immediately before the FSM call to close the dispatcher-vs-now TOCTOU window. Shipped. | 28 |
| 51  | empty-diff-as-transport-failure | 2026-05-10 morning: GLM-5.1-zai (and its Wafer fallback) silently returned rc=0 with empty stdout and zero worktree edits across 433 doer calls in 6h. Every checker call after that graded an empty diff, the triage misclassified the layer as `local_ci`/`rubric`/`transport` based on (empty) artifact content, and same-signature stop-loss tripped only after 10 attempts per subtask, costing ~10 hours of progress. Plan 51 separates content-class from transport-class failures: `_check_subtask` now detects `not self._last_diff_text` and synthesizes a FAIL outcome with a fixed prefix, skipping the LLM checker (no tokens spent grading nothing); `_record_subtask_triage` sniffs the prefix and skips the LLM triage call too, stamping `failure_layer="transport"` directly. New `_maybe_transport_stop_loss` runs before plan 23's same-signature check and fires after K (default 3) consecutive `,layer=transport` retries with a distinctly worded operator message naming the doer model swap as the recommended fix. New `cfg.subtask_transport_stop_loss_count` Field (default 3, ge=2, le=10) wired into `load_config` per plan 50's audit warns. New `subtask_empty_diff:<id>` artifact for `qk show` visibility. Shipped. | 23, 47, 48, 50 |
| 53  | fixup-planner-root-cause-tracing | 2026-05-10 morning: R-0040 / F-CI-1 incident. The fixup planner read GitHub CI's `generated-interface-contracts.ts is out of date. Run: pnpm contracts:generate` and emitted F-CI-1 instructing the doer to run that exact command. The doer's local container had cached `target/` + `node_modules` that masked the actual environmental drift the GitHub runner detected; `pnpm contracts:generate` produced no diff. Empty-diff cascade tripped plan-51's transport stop-loss after 3 attempts — wrong layer, wrong recovery action. Plan 53 ships five parts: (1) fixup-planner prompt requires per-subtask `root_cause_hypothesis` and walks the build graph upward instead of distilling the CI error verbatim; (2) post-PR FSM runs `local_ci_command` against worktree HEAD before invoking the planner and threads `local_ci_at_head=(passed, excerpt)` into the prompt for explicit three-case dispatch (GitHub fails AND local fails / passes / etc.); (3) doer prompt's §6a reproduce-before-fix rule for `kind="fixup_ci"` (clean rebuild + full chain, not just the final generator step); (4) worker positive empty-diff DONE path: empty diff + green objective gate + green witnesses → no-op DONE PASS for `kind != "fixup_ci"` (no commit, no push); → cannot_reproduce FAIL for `kind == "fixup_ci"` with new `failure_layer="cannot_reproduce"` enum and K=2 stop-loss naming environmental drift; (5) plan-52 migration F-CI-* heuristic fixed from hardcoded cycle 2 to `MAX(non-F-CI cycle on this task) + 1`. New `subtask_cannot_reproduce_stop_loss_count` Field (default 2, ge=2, le=10) plumbed in load_config + audit log. Shipped. | 33, 38, 47, 48, 51, 52 |
| 54  | no-op-done-ci-fix-context-fix | 2026-05-10 13:08 R-0015 crash-loop on `run_ci_fix_response`: plan 53's no-op-DONE path returns `kind="settled"` from `_handle_subtask_pass`, then `_handle_passed_subtask` unconditionally fires `enter_committing` (`SUBTASK_PASSED`) + `enter_pushing` (`COMMIT_CREATED`). In the CI-fix flow the parent task is in `ADDRESSING_FEEDBACK`; the FSM rejects `addressing_feedback → committing` so `InvalidTransition` raises, daemon recovers task to `PENDING_CI`, next CI poll re-emits the fixup_ci subtask, doer hits no-op-DONE again, same crash. Plan 54 gates the `enter_committing` / `enter_pushing` events in `_handle_passed_subtask` on the parent state being in the per-subtask-loop set `{DOING_SUBTASK, CHECKING_SUBTASK, TRIAGING_SUBTASK}`. When the parent is in `ADDRESSING_FEEDBACK` (or any other non-loop state) the events are skipped — the subtask DB row is already DONE; the feedback caller (`run_ci_fix_response` / `run_changes_requested_response`) handles the `FEEDBACK_PUSHED → PENDING_CI` transition itself. Plan-49 follow-up: `_run_fixup_round`'s `enter_fixup_planning` call had a TOCTOU race where the parent could drift out of `ADDRESSING_FEEDBACK` between the dispatcher's read and the FSM call. Re-read state right before the FSM call and only fire the event when the source state is actually valid (`LOCAL_CI_CHECKING` / `PRE_PR_AUDITING` / `FIXUP_PLANNING`); otherwise log + skip and let the next FSM tick re-evaluate. Shipped. | 49, 53 |
| 55  | fresh-container-per-audit-cycle + agent-self-clean | Plan 53 caught environmental drift reactively at K=2 (`failure_layer="cannot_reproduce"`); plan 55 preempts the class entirely. New `cfg.audit_fresh_container` (default off) re-provisions the dev container at the start of every pre-PR audit cycle. New `cfg.audit_bootstrap_command` (default empty) runs the project's clean-state command inside the fresh container; if the bootstrap produces drift, it auto-commits + pushes as `audit-bootstrap: cycle <N>` BEFORE the gauntlet runs. Bootstrap rc != 0 BLOCKs with a distinct `audit_bootstrap_failed` reason. Doer prompt §6a gains a paragraph naming `cfg.audit_bootstrap_command` so a `kind="fixup_ci"` doer can re-run the bootstrap mid-investigation when its local CI doesn't reproduce GitHub's failure. New `quikode/workers/audit_bootstrap.py` keeps the wire-up out of `pre_pr.py`'s line budget. Shipped. | 33, 38, 53 |
| 56  | auto-detect-merged-via-ancestry | The operator's sustained release-batch review pattern integrates N AWAITING_REVIEW PRs into a local integration branch, pushes that branch to main, then closes the constituent PRs with `gh pr close` — GitHub reports each PR as `state=CLOSED, merged=false` even though every commit is in main. Pre-plan-56 the daemon treated CLOSED-not-merged as the unhappy path and required a per-PR `qk mark-merged` to settle the audit trail. Plan 56 wires an ancestry check (`git merge-base --is-ancestor <branch_tip> origin/main`) into `_handle_closed_polled_pr` BEFORE the existing closed-without-merge handling: if commits ARE reachable from main, the task auto-transitions to MERGED via the same FSM call `qk mark-merged` uses. New `cfg.auto_detect_merged_via_ancestry` (default on; opt out for regulated workflows). New shared primitive `quikode/ancestry.py` (`branch_is_ancestor_of_main(run_git, ...)`) so the worker (running git inside the per-task container) and the new `qk detect-merged` CLI (running host git via `subprocess.run`) share identical semantics. The CLI is dry-run by default and prints a per-task ancestry-status report; `--apply` fires the auto-merge. `fsm_runtime.mark_merged` gains side-state / terminal-state bridges (ADDRESSING_FEEDBACK / REBASING_TO_MAIN → BLOCKED → PENDING → MERGED; BLOCKED / FAILED / ABORTED → PENDING → MERGED via RETRY_TASK + MARK_MERGED) so retroactive `qk detect-merged --apply` sweeps cover tasks that drifted into post-PR side states between push and sweep. Shipped. | 28, 30 |
| 52  | replan-last-cycle-primitive | New `qk replan-cycle <task>` recovery primitive that sits between `qk rewind` (one subtask) and `qk retry` (whole task) in the escalation ladder. Reframes orientation §3 so retry becomes a last-resort reserved for "even the initial decomposition was wrong-shape"; cycle-level non-convergence in fixup / replan / merge cycles routes to replan-cycle, which preserves every earlier-cycle commit + retry counter. Schema migration adds `subtasks.planning_cycle` + `subtasks.planning_kind` (with subtask-id heuristic backfill of pre-plan-52 rows) plus `tasks.replan_cycle_marker`. Each planner emit-site (initial / fixup / replan / merge) tags its output with `(cycle, kind)`. CLI deletes only the latest cycle's rows, force-pushes the branch back to before the cycle's first commit, clears `pre_pr_audit_summary`, sets the marker, transitions to PENDING with `resume_from_existing_subtasks=1` so the worker reuses preserved `plan_text`. Worker re-fire is implicit: with cycle-N rows deleted the natural fixup / replan / merge flow re-emits at the same ordinal (next emission increments from N-1 back to N). Comes with `--dry-run`. Orientation §3.1–§3.6 + runbook-operations.md interventions section + runbook-incident-response.md "PR Closed" / "Rebase Conflict Unresolved" / "Feedback Cap Hit" all updated. Tests cover CLI behavior (8 paths), planner-cycle emit, append-with-explicit-cycle replacement, migration backfill heuristic + idempotency. Shipped. | 27, 38 |
| 57  | typed-fsm-enter-guards | Plans 49 + 54 patched specific `enter_*` call sites with state-check guards after `InvalidTransition: cannot enter X from Y` crashes; the pattern multiplies per new call site or new state. Plan 57 generalizes by pushing the state-check INTO each `fsm_runtime.enter_*` helper, plus `mark_merged` and `block_current`. The helpers now return `State | None` and silently skip with an INFO log naming the helper + the current state when the source state doesn't allow the requested transition. The underlying `store.apply_event` still raises (only the helper layer relaxes). Plan-49 / plan-54 call-site guards stay as defense-in-depth (cheaper, clearer at the call site); the FSM-layer no-op is the safety net. `_safe_crash_current` in `task_worker.py` keeps its `except InvalidTransition` per plan 20 (1C — `crash_current` is intentionally not relaxed). Foundational for plan 58 (5 new states + transition reshape would otherwise need proportionally more per-call-site guards). New test file `tests/test_fsm_runtime_typed_guards.py` parametrizes invalid + valid cases across every `enter_*` helper plus `mark_merged` / `block_current`. Shipped — manager handles deploy paired with plan 58. | 49, 54 |

## Suggested ship order (no priorities crossing each other)

1. **02 + 04 + 07A** (heartbeat watch). Pure additions, no semantics change. Highest
   leverage during overnight runs; lowest risk.
2. **03**. Two-line config wiring + plan-02-dependent retry-with-backoff.
3. **06 + 09 (A, B)**. Detection upgrade + prompt edits land together; one reinstall.
4. **05**. Wipe-and-replan FSM event. Only after 06 ships, since 05 reuses 06's
   locality fingerprint.
5. **08**. Tighten budgets once the safer detectors are in place.
6. **10**. Recovery hardening — surgical, needs care.

## Things explicitly NOT planned

- Multi-host parallelism (the user is single-host).
- Switching agent CLIs around mid-run (existing config supports it, no plan needed).
- DAG editor / planner that proposes new nodes (out of scope for quikode-the-runner).
- Tanren-specific code edits (constraint: state-machine and prompts only).
